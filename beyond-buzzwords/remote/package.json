{
  "name": "beyond-buzzwords-remote",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "synth": "npm run build && cdk synth -a \"node dist/bin/remote.js\"",
    "deploy": "npm run build && cdk deploy -a \"node dist/bin/remote.js\"",
    "destroy": "npm run build && cdk destroy -a \"node dist/bin/remote.js\""
  },
  "dependencies": {
    "aws-cdk-lib": "^2.245.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "aws-cdk": "^2.190.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  }
}
